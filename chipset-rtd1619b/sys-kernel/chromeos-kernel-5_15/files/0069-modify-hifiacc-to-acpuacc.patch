From e4c6d0bb00b0989ee576b68adcafcb96c6a030a3 Mon Sep 17 00:00:00 2001
From: yaozenhu <yaozenhu@realtek.com>
Date: Wed, 27 Mar 2024 11:33:46 +0800
Subject: [PATCH 69/69] modify-hifiacc-to-acpuacc

---
 sound/soc/realtek/common/snd-hifi-realtek.c | 14 +++++++-------
 1 file changed, 7 insertions(+), 7 deletions(-)

diff --git a/sound/soc/realtek/common/snd-hifi-realtek.c b/sound/soc/realtek/common/snd-hifi-realtek.c
index 43e2b2ddc..562427881 100644
--- a/sound/soc/realtek/common/snd-hifi-realtek.c
+++ b/sound/soc/realtek/common/snd-hifi-realtek.c
@@ -491,7 +491,7 @@ static int rtk_snd_init_ringheader_AI(struct snd_pcm_runtime *runtime)
 	for (ch = 0; ch < runtime->channels; ch++) {
 		ret = rtk_snd_alloc(dev, size, (void *)&dat, (void **)&vaddr,
 				    RTK_FLAG_NONCACHED | RTK_FLAG_SCPUACC |
-				    RTK_FLAG_HIFIACC);
+				    RTK_FLAG_ACPUACC);
 		if (ret) {
 			pr_err("%s alloc mem fail\n", __func__);
 			return ret;
@@ -541,7 +541,7 @@ static int rtk_snd_capture_LPCM_ringheader_AI(struct snd_pcm_runtime *runtime)
 
 	ret = rtk_snd_alloc(dev, size, (void *)&dat, (void **)&vaddr,
 			    RTK_FLAG_NONCACHED | RTK_FLAG_SCPUACC |
-			    RTK_FLAG_HIFIACC);
+			    RTK_FLAG_ACPUACC);
 	if (ret) {
 		pr_err("%s alloc mem fail\n", __func__);
 		return ret;
@@ -589,7 +589,7 @@ static int rtk_snd_capture_PTS_ringheader_AI(struct snd_pcm_runtime *runtime)
 
 	ret = rtk_snd_alloc(dev, size, (void *)&dat, (void **)&vaddr,
 			    RTK_FLAG_NONCACHED | RTK_FLAG_SCPUACC |
-			    RTK_FLAG_HIFIACC);
+			    RTK_FLAG_ACPUACC);
 	if (ret) {
 		pr_err("%s alloc mem fail\n", __func__);
 		return ret;
@@ -641,7 +641,7 @@ static int rtk_snd_init_ringheader_DEC_AO(struct snd_pcm_runtime *runtime)
 	for (ch = 0; ch < runtime->channels; ch++) {
 		ret = rtk_snd_alloc(dev, size, (void *)&dat, (void **)&vaddr,
 				    RTK_FLAG_NONCACHED | RTK_FLAG_SCPUACC |
-				    RTK_FLAG_HIFIACC);
+				    RTK_FLAG_ACPUACC);
 		if (ret) {
 			pr_err("%s alloc mem fail\n", __func__);
 			return ret;
@@ -706,7 +706,7 @@ static int rtk_snd_init_ringheader_AO(struct snd_pcm_runtime *runtime)
 
 	ret = rtk_snd_alloc(dev, size, (void *)&dat, (void **)&vaddr,
 			    RTK_FLAG_NONCACHED | RTK_FLAG_SCPUACC |
-			    RTK_FLAG_HIFIACC);
+			    RTK_FLAG_ACPUACC);
 	if (ret) {
 		pr_err("%s alloc mem fail\n", __func__);
 		return ret;
@@ -803,7 +803,7 @@ static int rtk_snd_init_decoder_inring(struct snd_pcm_runtime *runtime)
 
 	ret = rtk_snd_alloc(dev, size, (void *)&dat, (void **)&vaddr,
 			    RTK_FLAG_NONCACHED | RTK_FLAG_SCPUACC |
-			    RTK_FLAG_HIFIACC);
+			    RTK_FLAG_ACPUACC);
 	if (ret) {
 		pr_err("%s alloc mem fail\n", __func__);
 		return ret;
@@ -839,7 +839,7 @@ static int rtk_snd_init_decoder_inring(struct snd_pcm_runtime *runtime)
 
 	ret = rtk_snd_alloc(dev, size, (void *)&dat, (void **)&vaddr,
 			    RTK_FLAG_NONCACHED | RTK_FLAG_SCPUACC |
-			    RTK_FLAG_HIFIACC);
+			    RTK_FLAG_ACPUACC);
 	if (ret) {
 		pr_err("%s alloc mem fail\n", __func__);
 		return ret;
-- 
2.42.0

